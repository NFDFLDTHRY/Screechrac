ngth} baseline + ${raw.active.length} active events.`};
}

const SLIDES = [
{id:'session',title:'Session / HTTPS',goal:'Establish secure context, origin, UA, isolation, and the monotonic page clock before touching sensors.',run:async()=>{const raw={secureContext:isSecureContext,origin:location.origin,href:location.href,userAgent:navigator.userAgent,crossOriginIsolated,hardwareConcurrency:navigator.hardwareConcurrency??null,deviceMemory:navigator.deviceMemory??null,language:navigator.language,platform:navigator.platform,performanceNow:+perf().toFixed(3),timeOrigin:performance.timeOrigin};state.session.environment=raw;return {raw,normalized:{exists:true,constructs:true,starts:true,emits:true,responds:true,verdict:isSecureContext?'CHARACTERIZED':'BLOCKED'},summary:`secureContext=${isSecureContext}; origin=${location.origin}`};}},
{id:'inventory',title:'Browser surface inventory',goal:'Reconnaissance only. Record expected and unexpected camera/spatial surfaces before requesting permission or starting sensors.',run:async()=>{const expected=['Accelerometer','LinearAccelerationSensor','GravitySensor','Gyroscope','Magnetometer','UncalibratedMagnetometer','AbsoluteOrientationSensor','RelativeOrientationSensor','DeviceMotionEvent','DeviceOrientationEvent','MediaStreamTrackProcessor','ImageCapture','VideoFrame'];const expectedMap=Object.fromEntries(expected.map(n=>[n,globalSurface(n)]));const raw={expected:expectedMap,nearbyGlobals:nearbyGlobals(),mediaDevices:objectShape(navigator.mediaDevices),videoPrototypeHasRVFC:'requestVideoFrameCallback' in HTMLVideoElement.prototype,permissionsPolicyAPI:{permissionsPolicy:'permissionsPolicy' in document,featurePolicy:'featurePolicy' in document}};const unexpected=raw.nearbyGlobals.filter(n=>!expected.includes(n));raw.unexpectedSurfaces=unexpected;return {raw,normalized:{exists:true,constructs:true,starts:true,emits:true,responds:true,verdict:'CHARACTERIZED'},summary:`${expected.filter(n=>expectedMap[n].exists).length}/${expected.length} expected surfaces exposed; ${unexpected.length} nearby/unexpected names discovered.`};}},
{id:'camera-api',title:'Camera API',goal:'Interrogate mediaDevices, getUserMedia, enumerateDevices, and supported video constraints without opening the camera.',run:async()=>{const raw={mediaDevices:!!navigator.mediaDevices,getUserMedia:typeof navigator.mediaDevices?.getUserMedia==='function',supportedConst