raints:navigator.mediaDevices?.getSupportedConstraints?.()||null,devices:null,error:null};try{raw.devices=(await navigator.mediaDevices.enumerateDevices()).map(d=>({kind:d.kind,label:d.label,deviceId:d.deviceId,groupId:d.groupId}));}catch(e){raw.error=compactError(e);}const pass=raw.mediaDevices&&raw.getUserMedia;return {raw,normalized:{exists:pass,constructs:pass,starts:false,emits:false,responds:false,verdict:pass?'EXPOSED':'ABSENT'},summary:`getUserMedia ${pass?'is exposed':'is absent'}; supported constraint keys=${raw.supportedConstraints?Object.keys(raw.supportedConstraints).length:0}.`};}},
{id:'camera-open',title:'Camera admission',goal:'Actually open the rear camera and prove a live MediaStreamTrack exists.',run:async()=>{setPhase('GRANT CAMERA ACCESS','Chrome may ask for permission.');const raw={};try{const {track}=await getCamera();await waitVideoReady();Object.assign(raw,{label:track.label,readyState:track.readyState,muted:track.muted,videoWidth:video.videoWidth,videoHeight:video.videoHeight});return {raw,normalized:{exists:true,constructs:true,starts:true,emits:track.readyState==='live',responds:true,verdict:track.readyState==='live'?'CHARACTERIZED':'STARTED'},summary:`${track.label||'camera'} live at ${video.videoWidth}×${video.videoHeight}.`};}catch(e){raw.error=compactError(e);return {raw,normalized:{exists:true,constructs:true,starts:false,emits:false,responds:false,verdict:e?.name==='NotAllowedError'||e?.name==='SecurityError'?'BLOCKED':'FAIL'},summary:`Camera open failed: ${e?.name}: ${e?.message}`};}}},
{id:'camera-settings',title:'Camera settings',goal:'Record every field Chrome actually returns from MediaStreamTrack.getSettings().',run:async()=>{try{const {track}=await getCamera();const settings=track.getSettings?.()||null;return {raw:{settings,shape:objectShape(settings)},normalized:{exists:!!track.getSettings,constructs:true,starts:true,emits:true,responds:true,verdict:settings?'CHARACTERIZED':'PARTIAL'},summary:`Chrome returned ${settings?Object.keys(settings).length:0} setting fields.`};}catch(e){return failure(e);}}},
{id:'camera-capabilities',title:'Camera capabilities',goal:'Record full getCapabilities() output, including fields we did not predict.',run:async()=>{try{const {track}=await getCamera();const caps=track.getCapabilities?.()||null;const known=['aspectRatio','deviceId','facingMode','frameRate','groupId','height','resizeMo