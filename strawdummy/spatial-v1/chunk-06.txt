de','width','torch','zoom','focusMode','focusDistance','exposureMode','exposureTime','exposureCompensation','whiteBalanceMode','colorTemperature','iso'];const keys=caps?Object.keys(caps):[];return {raw:{capabilities:caps,capabilityKeys:keys,unexpectedKeys:keys.filter(k=>!known.includes(k))},normalized:{exists:!!track.getCapabilities,constructs:true,starts:true,emits:true,responds:true,verdict:caps?'CHARACTERIZED':'PARTIAL'},summary:`${keys.length} capability fields; unexpected=${keys.filter(k=>!known.includes(k)).join(', ')||'none'}.`};}catch(e){return failure(e);}}},
{id:'pixels',title:'Raw camera pixels',goal:'Prove real RGBA bytes, then physically cover the lens and verify the pixel stream responds.',run:async()=>{try{await getCamera();await waitVideoReady();const c=document.createElement('canvas');c.width=64;c.height=36;const ctx=c.getContext('2d',{willReadFrequently:true});const sample=()=>{ctx.drawImage(video,0,0,c.width,c.height);const d=ctx.getImageData(0,0,c.width,c.height).data;let sum=0,h=2166136261;for(let i=0;i<d.length;i+=4){sum+=(d[i]+d[i+1]+d[i+2])/3;h^=d[i];h=Math.imul(h,16777619);h^=d[i+1];h=Math.imul(h,16777619);h^=d[i+2];h=Math.imul(h,16777619);}return {pageTimeMs:+perf().toFixed(3),meanLuma:+(sum/(d.length/4)).toFixed(3),hash32:h>>>0,center:Array.from(d.slice(((18*64+32)*4),((18*64+32)*4)+4))};};setPhase('POINT CAMERA AT THE SCENE','Sampling uncovered pixels for 2 seconds.');const open=[];for(let i=0;i<12;i++){open.push(sample());await sleep(120);}setPhase('COVER THE CAMERA LENS','Keep it covered for 2 seconds.');await sleep(700);const covered=[];for(let i=0;i<12;i++){covered.push(sample());await sleep(120);}const om=open.reduce((a,s)=>a+s.meanLuma,0)/open.length, cm=covered.reduce((a,s)=>a+s.meanLuma,0)/covered.length;const raw={path:'MediaStream -> HTMLVideoElement -> Canvas2D.drawImage -> getImageData',open,covered,openMean:+om.toFixed(3),coveredMean:+cm.toFixed(3),lumaDelta:+Math.abs(om-cm).toFixed(3),distinctHashes:new Set([...open,...covered].map(s=>s.hash32)).size};const responds=raw.lumaDelta>3||raw.distinctHashes>4;return {raw,normalized:{exists:true,constructs:true,starts:true,emits:true,responds,verdict:responds?'CHARACTERIZED':'EMITTING'},summary:`Pixel path live. Luma delta=${raw.lumaDelta}; distinct hashes=${raw.distinctHashes}.`};}catch(e){return failure(e);}}},
{id:'rvfc',title:'Video frame callback',goal:'Characterize re