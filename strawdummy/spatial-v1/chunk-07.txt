questVideoFrameCallback cadence and timestamp behavior on the live camera.',run:async()=>{try{await getCamera();await waitVideoReady();if(!('requestVideoFrameCallback' in HTMLVideoElement.prototype))return absent('requestVideoFrameCallback absent');const samples=[];await new Promise((resolve,reject)=>{const to=setTimeout(()=>resolve(),4000);const cb=(t,m)=>{samples.push({pageTimeMs:+perf().toFixed(3),callbackNow:+t.toFixed(3),mediaTime:m.mediaTime,presentedFrames:m.presentedFrames,expectedDisplayTime:m.expectedDisplayTime,width:m.width,height:m.height});if(samples.length>=60){clearTimeout(to);resolve();return;}video.requestVideoFrameCallback(cb);};video.requestVideoFrameCallback(cb);});const stats=summarizeSamples(samples,['mediaTime']);return {raw:{samples,stats},normalized:{exists:true,constructs:true,starts:true,emits:samples.length>0,responds:samples.length>5,verdict:samples.length>5?'CHARACTERIZED':'PARTIAL'},summary:`Received ${samples.length} video frame callbacks; observed callback rate ${stats.observedHz||'n/a'} Hz.`};}catch(e){return failure(e);}}},
{id:'track-processor',title:'MediaStreamTrackProcessor',goal:'Test whether Chrome exposes the lower-level track processor and whether it yields an actual VideoFrame.',run:async()=>{const raw={surface:globalSurface('MediaStreamTrackProcessor')};if(!raw.surface.exists)return absent('MediaStreamTrackProcessor absent',raw);try{const {track}=await getCamera();const p=new MediaStreamTrackProcessor({track});raw.processorShape=objectShape(p);const reader=p.readable.getReader();const result=await Promise.race([reader.read(),sleep(3000).then(()=>({timeout:true}))]);if(result.timeout){try{reader.cancel()}catch{};return {raw,normalized:{exists:true,constructs:true,starts:true,emits:false,responds:false,verdict:'STARTED'},summary:'Processor constructed but no frame arrived within 3 seconds.'};}const frame=result.value;raw.frame={timestamp:frame?.timestamp??null,duration:frame?.duration??null,codedWidth:frame?.codedWidth??null,codedHeight:frame?.codedHeight??null,format:frame?.format??null};try{frame?.close()}catch{};try{reader.cancel()}catch{};return {raw,normalized:{exists:true,constructs:true,starts:true,emits:!!frame,responds:!!frame,verdict:frame?'CHARACTERIZED':'PARTIAL'},summary:frame?`Yielded VideoFrame ${raw.frame.codedWidth}×${raw.frame.codedHeight}.`:'No VideoFrame yielded.'};}catch(e){raw.error=compactErr