or(e);return {raw,normalized:{exists:true,constructs:false,starts:false,emits:false,responds:false,verdict:e?.name==='SecurityError'?'BLOCKED':'FAIL'},summary:`TrackProcessor failed: ${e?.name}: ${e?.message}`};}}},
{id:'image-capture',title:'ImageCapture',goal:'Probe the still-capture/control surface Chrome exposes on the live camera track.',run:async()=>{const raw={surface:globalSurface('ImageCapture')};if(!raw.surface.exists)return absent('ImageCapture absent',raw);try{const {track}=await getCamera();const ic=new ImageCapture(track);raw.shape=objectShape(ic);try{raw.photoCapabilities=await ic.getPhotoCapabilities?.();}catch(e){raw.photoCapabilitiesError=compactError(e);}try{raw.photoSettings=await ic.getPhotoSettings?.();}catch(e){raw.photoSettingsError=compactError(e);}return {raw,normalized:{exists:true,constructs:true,starts:true,emits:!!(raw.photoCapabilities||raw.photoSettings),responds:true,verdict:'CHARACTERIZED'},summary:`ImageCapture constructed; photoCapabilities=${!!raw.photoCapabilities}; photoSettings=${!!raw.photoSettings}.`};}catch(e){raw.error=compactError(e);return {raw,normalized:{exists:true,constructs:false,starts:false,emits:false,responds:false,verdict:e?.name==='SecurityError'?'BLOCKED':'FAIL'},summary:`ImageCapture failed: ${e?.name}: ${e?.message}`};}}},
{id:'accelerometer',title:'Accelerometer',goal:'Generic Sensor API path. Hold still, then shake once. Prove readings physically respond.',run:()=>twoPhaseGenericSensor('Accelerometer','SHAKE PHONE ONCE',['x','y','z'])},
{id:'linear-accel',title:'Linear acceleration',goal:'Probe LinearAccelerationSensor independently. Hold still, then move phone sharply sideways.',run:()=>twoPhaseGenericSensor('LinearAccelerationSensor','MOVE PHONE SIDEWAYS',['x','y','z'])},
{id:'gravity',title:'Gravity sensor',goal:'Probe GravitySensor independently. Hold still, then turn the phone face down.',run:()=>twoPhaseGenericSensor('GravitySensor','TURN PHONE FACE DOWN',['x','y','z'])},
{id:'gyroscope',title:'Gyroscope',goal:'Generic Sensor gyro path. Hold still, then rotate phone left and right.',run:()=>twoPhaseGenericSensor('Gyroscope','ROTATE LEFT / RIGHT',['x','y','z'])},
{id:'device-motion',title:'DeviceMotion event path',goal:'Probe the older Chromium/Android motion event and preserve all useful motion fields.',run:()=>twoPhaseEvent('devicemotion','SHAKE + ROTATE PHONE',e=>({x:e.acceleration?.x??nul