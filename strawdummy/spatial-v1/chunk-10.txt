ssions',title:'Permission surfaces',goal:'Ask navigator.permissions only what it will answer, one descriptor at a time. Unsupported descriptors are evidence.',run:async()=>{const names=['camera','microphone','accelerometer','gyroscope','magnetometer'];const raw={};for(const name of names){try{const p=await navigator.permissions.query({name});raw[name]={state:p.state,shape:objectShape(p)};}catch(e){raw[name]={error:compactError(e)};}}return {raw,normalized:{exists:!!navigator.permissions,constructs:true,starts:true,emits:true,responds:true,verdict:'CHARACTERIZED'},summary:`Queried ${names.length} permission descriptors; ${names.filter(n=>raw[n]?.state).length} returned states.`};}},
{id:'policy',title:'Permissions Policy',goal:'Distinguish API absence from policy blocking by interrogating document.permissionsPolicy / featurePolicy when exposed.',run:async()=>{const raw={permissionsPolicy:null,featurePolicy:null};try{if(document.permissionsPolicy){raw.permissionsPolicy={shape:objectShape(document.permissionsPolicy),allowedFeatures:document.permissionsPolicy.allowedFeatures?.()||null};}}catch(e){raw.permissionsPolicy={error:compactError(e)}}try{if(document.featurePolicy){raw.featurePolicy={shape:objectShape(document.featurePolicy),allowedFeatures:document.featurePolicy.allowedFeatures?.()||null,features:document.featurePolicy.features?.()||null};}}catch(e){raw.featurePolicy={error:compactError(e)}}const exposed=!!(document.permissionsPolicy||document.featurePolicy);return {raw,normalized:{exists:exposed,constructs:exposed,starts:exposed,emits:exposed,responds:exposed,verdict:exposed?'CHARACTERIZED':'ABSENT'},summary:exposed?'Permissions/Feature Policy surface exposed.':'No policy inspection surface exposed on document.'};}},
{id:'timestamps',title:'Timestamp audit',goal:'Compare timestamp domains and monotonicity from every successful prior slide without inventing equivalence.',run:async()=>{const rows=[];for(const [id,entry] of Object.entries(state.session.slides)){for(const [i,a] of (entry.attempts||[]).entries()){const r=a.raw||{};const arrays=[];for(const k of ['samples','baseline','active','open','covered'])if(Array.isArray(r[k]))arrays.push([k,r[k]]);for(const [k,arr] of arrays){const page=arr.map(x=>x.pageTimeMs).filter(Number.isFinite);const ev=arr.map(x=>x.eventTimeStamp).filter(Number.isFinite);const st=arr.map(x=>x.sensorTimestamp).filter(Number.isFi