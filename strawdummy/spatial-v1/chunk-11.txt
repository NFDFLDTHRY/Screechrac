nite);const mt=arr.map(x=>x.mediaTime).filter(Number.isFinite);rows.push({slide:id,attempt:i,array:k,count:arr.length,pageTime:{count:page.length,monotonic:page.every((v,j)=>!j||v>=page[j-1]),first:page[0]??null,last:page.at(-1)??null},eventTime:{count:ev.length,monotonic:ev.every((v,j)=>!j||v>=ev[j-1]),first:ev[0]??null,last:ev.at(-1)??null},sensorTimestamp:{count:st.length,monotonic:st.every((v,j)=>!j||v>=st[j-1]),first:st[0]??null,last:st.at(-1)??null},mediaTime:{count:mt.length,monotonic:mt.every((v,j)=>!j||v>=mt[j-1]),first:mt[0]??null,last:mt.at(-1)??null}});}}}return {raw:{rows},normalized:{exists:true,constructs:true,starts:true,emits:true,responds:true,verdict:'CHARACTERIZED'},summary:`Audited ${rows.length} prior sample arrays. No cross-domain equivalence assumed.`};}},
{id:'summary',title:'Capability summary',goal:'Build the normalized treasure map from accepted prior slide results only. Raw evidence remains untouched.',run:async()=>{const map={};for(const s of SLIDES){if(['summary','export'].includes(s.id))continue;const a=accepted(s.id);map[s.id]=a?{verdict:a.normalized?.verdict,normalized:a.normalized,summary:a.summary}: {verdict:'NOT_SAVED'};}return {raw:{capabilityMap:map},normalized:{exists:true,constructs:true,starts:true,emits:true,responds:true,verdict:'CHARACTERIZED'},summary:`Built capability map from ${Object.values(map).filter(x=>x.verdict!=='NOT_SAVED').length} saved slide results.`};}},
{id:'export',title:'Export evidence',goal:'Freeze the complete raw + normalized session evidence into one JSON file.',run:async()=>{state.session.exportedAt=nowISO();save();download();return {raw:{sessionId:state.session.sessionId,eventCount:state.session.events.length,slideCount:Object.keys(state.session.slides).length},normalized:{exists:true,constructs:true,starts:true,emits:true,responds:true,verdict:'PASS'},summary:'Evidence JSON exported.'};}}
];

function failure(e){return {raw:{error:compactError(e)},normalized:{exists:true,constructs:false,starts:false,emits:false,responds:false,verdict:e?.name==='SecurityError'||e?.name==='NotAllowedError'?'BLOCKED':'FAIL'},summary:`${e?.name||'Error'}: ${e?.message||e}`};}
function absent(summary='Surface absent',raw={}){return {raw,normalized:{exists:false,constructs:false,starts:false,emits:false,responds:false,verdict:'ABSENT'},summary};}

async function runCurrent(){
  cleanupActive(); const s=SLIDES[s